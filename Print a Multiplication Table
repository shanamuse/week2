#include <stdio.h>

int main() {
    int number;

    printf("Please enter a whole number:\n\n");
    scanf("%d", &number);

    for (int i = 1; i <= 12; i++) {
        printf("%d x %d = %d\n", i, number, i * number);
    }

    return 0;
}
